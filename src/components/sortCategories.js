import { View, StyleSheet } from "react-native";

export default function SortCategories() {
    return (
        <View style={styles.container}>
            {/* Không còn bất kỳ danh mục nào */}
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',
        
    },
});
